views:
  - type: table
    name: Table
    filters:
      and:
        - "!name.isEmpty()"
    order:
      - file.name
      - forge
      - repo
      - name
      - versions
    sort:
      - property: tags
        direction: ASC
      - property: versions
        direction: DESC
      - property: partner
        direction: DESC
